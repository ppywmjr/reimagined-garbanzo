{
  "$schema": "https://schema.railpack.com",
  "secrets": ["DATABASE_URL", "CLERK_SECRET_KEY", "INTERNAL_API_SECRET"],
  "steps": {
    "build": {
      "secrets": ["*"],
      "commands": ["npm run build:railpack"]
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}